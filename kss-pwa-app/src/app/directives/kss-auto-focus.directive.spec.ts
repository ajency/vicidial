import { KssAutoFocusDirective } from './kss-auto-focus.directive';

describe('KssAutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new KssAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
